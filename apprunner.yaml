version: 1.0
runtime: python311 
build:
  commands:
    build:
      - apt-get install tmate
      - pip3 install streamlit
      - echo $PATH
      - which python || true
      - which python3 || true
      - which pip || true
      - which pip3 || true
run: 
  command: tmate 
  network:
    port: 9000
